class Solution:
    def buildArray(self, nums: List[int]) -> List[int]:
        a=[0]*len(nums)
        b=[]
        for i in range(0,len(nums)):
            a=nums[nums[i]]
            b.append(a)
        return b
                        
        
