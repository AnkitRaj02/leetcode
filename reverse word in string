class Solution:
    def reverseWords(self, s: str) -> str:
        a=s.split(" ")
        b=""
        for i in a:
            b+=i[::-1 ]+ " "
            c=" ".join(b.split())
            
        return c
            
        
